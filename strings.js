let amanmsg="meet me after class     "
//string are immutable(it cannot be changed)

//repeat the strings for n times
console.log(amanmsg.repeat(3));

let message="hmm    "

//trim the extra spaces
console.log(message.trim());
